<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fournir l'effet de feedback visuel lors de la sélection -->
    <item android:drawable="?android:attr/selectableItemBackground" />

    <!-- Bordure en bas -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <!-- Définir uniquement la bordure en bas -->
            <stroke android:width="0dp" android:color="@android:color/transparent" />
            <padding
                android:top="-2dp"
                android:left="-2dp"
                android:right="-2dp"
                android:bottom="1dp" /> <!-- La bordure apparaît comme un padding en bas -->
        </shape>
    </item>

    <!-- Bordure en bas avec la couleur spécifiée -->
    <item android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="1dp"
                android:color="@color/gray" /> <!-- Changez @color/gray à la couleur de bordure désirée -->
        </shape>
    </item>
</layer-list>
